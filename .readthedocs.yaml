version: 2

build:
  os: "ubuntu-22.04"
  tools:
    python: "3.12"

system_packages:
  apt_packages:
    - build-essential
    - libzmq3-dev 
    - gcc
    - g++
    - python3-dev

python:
  install:
    - requirements: requirements-docs.txt
    
sphinx:
  configuration: docs/conf.py 
